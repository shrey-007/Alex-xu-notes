- DESIGN A UNIQUE ID GENERATOR IN DISTRIBUTED SYSTEMS -: the solution is auto-increment, just start from 1 and keep on increasing, thus works fine in one one server, but in distributed systems , it creates problem. Because suppose aerver A has current id as 5, now request comes to generate new id, so it increments to 6, but 6 is already generated by server B, and there are many servers, so we can't check with all servers, so that's a problem
- we have already studied the web crawler chapter, starting from seed url, we do bfs/dfs, and maintain visited nodes and and robots.txt, spider traps etc.
- In the design of notification system, notification server puts the notification in the message queue, as it may take time for 3rd party like FCM to send the notification. Also the notification can fail as well, so store them in db and do retry process, check that same notification may not reach user twice using idempotency
-   
